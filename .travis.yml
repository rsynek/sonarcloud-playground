language: java
sudo: false
install: true

jdk:
- oraclejdk8
script:
- mvn --quiet clean install -Prun-code-coverage
- mvn --quiet jacoco:report jacoco:merge sonar:sonar -Preport-code-coverage -Dsonar.host.url=https://sonarcloud.io -Dsonar.organization=rsynek -Dsonar.login=$SONARCLOUD_TOKEN -Dsonar.projectKey=sonarcloud-playground
cache:
  directories:
  - "$HOME/.m2/repository"
  - "$HOME/.sonar/cache"